# 給 Codex：用問答方式把「8 槽位相簿模板」改成我的版本

原始模板 Repo：<https://github.com/EverydayBuildLab/photo-album-template>

把下面整段直接貼給 Codex 即可。

---

你現在要協助我把這個 Google Drive 相簿 DEMO 客製成我自己的網站。

**工作原則：先問答釐清，再真正做到完成；不要只給規劃，也不要一開始就亂改。**

## 0. 先保護原始模板

1. 先確認目前工作目錄是使用 **Use this template** 建立的我自己的新 Repo，或至少是我自己的 Clone。
2. 如果還沒有自己的 Repo，優先引導我在 GitHub 使用 **Use this template → Create a new repository**；不要把一般客製流程做成 Fork。
3. **不可修改模板來源 Repo。**所有修改只能發生在我自己的新 Repo / Clone。
4. 預設只使用 `main` branch，不要為一般客製建立一堆 branches，除非我明確要求。

## 1. 先理解模板：最多 8 個固定槽位，不是固定四分類

模板在 `site-config.js` 的 `folderSlots` 預留 **01～08 共 8 個邏輯槽位**。

- `enabled: true`：網站顯示這個分類。
- `enabled: false`：保留槽位但不顯示。
- 目前 DEMO 只是啟用前四個：大家、個人、影片、AI後製。
- 使用者可改成 1～8 個分類，不必永遠保留這四個名稱。

**Google Drive 資料夾名稱不是程式綁定鍵。**每個槽位以 `folderId` 綁定真實資料夾，因此 Drive 資料夾可自由改名。網站按鈕顯示名稱由 `label` 控制；如果使用者也要改網站名稱，才同步改 `label`。

不要寫出「資料夾一定要叫 01-大家」這種限制。槽位序號是模板邏輯位置；Drive 名稱只是人看的名稱。

## 2. 用問答方式收集需求

請一輪一輪問我，**每輪最多 3 個問題**。每輪得到答案後先整理目前決定，資訊不足再問下一輪。

至少確認：

### A. 網站定位

- 相簿主題是什麼？
- 網站標題、副標、簡短說明要寫什麼？
- 是否保留 DEMO / 公開展示字樣？

### B. 分類槽位 1～8

先問我要幾個分類（1～8），再逐類確認：

- 網站顯示名稱 `label`
- 圖片還是影片
- 單位（張／部／其他）
- 素材數量
- 是否需要 AI 提示詞功能

如果我說某分類是「AI後製」或其他 AI 風格分類，請確認每一張是否都要有提示詞。需要提示詞時，每筆資料都必須有 `prompt`，網站要能預覽與複製。

### C. Google Drive

- 我要使用哪一個 Google Drive 根資料夾？請我提供分享網址。
- 網站標題右側保留「所有照片」按鈕，固定開啟這個根資料夾；不要改成只開目前分類的子資料夾。
- 對每一個啟用槽位，確認或建立一個對應的 Drive 子資料夾。
- **以真實 `folderId` 綁定槽位，不以資料夾名稱綁定。**
- 資料夾名稱讓使用者自行命名；不要要求名稱一定等於網站分類名稱。
- 取得每筆媒體真正的 Google Drive `fileId`，不能用本機假圖／假影片冒充。
- 公開網站必須能讀取媒體，同時提醒不要把私人資料誤設成公開。

### D. 密碼與功能

- 進站密碼要設什麼？
- 預設記住登入 90 天：只能保存不可逆密碼摘要＋到期時間，不能把明文密碼寫進 localStorage；必須提供「登出」清除記憶。密碼設定改變後，舊記憶需自動失效。
- 預設保留：分類切換、最新／最舊、Lightbox、上一個／下一個、下載、影片播放器、手機響應式。
- 若分類需要 AI 提示詞，保留提示詞預覽與複製。
- 預設保留 10 套變換配色，使用 `localStorage` 記住配色。

提醒：純前端密碼只算展示門檻，不是真正安全保護。

### E. 視覺方向

用白話問我：清爽明亮／深色質感／溫暖生活／極簡中性／其他；以及手機上最在意照片大小、按鈕、分類、文字密度哪一項。

### F. 部署

問我要：

1. GitHub Pages（預設）
2. Cloudflare Pages / Workers
3. Netlify / Vercel
4. 只要本機

沒有偏好時優先 GitHub Pages。

## 3. 問完先給「確認摘要」

開始改檔前，用很短的方式整理：

- 主題／標題
- 啟用幾個槽位
- 每個槽位：序號、網站名稱、圖片／影片、數量、是否需提示詞、對應 Drive 資料夾
- 密碼／功能
- 視覺
- 部署

只問一次：「以上是否正確？若正確我就直接做到完成。」

確認後，不要每一步停下來問是否繼續，除非真的遇到登入、授權或缺資料的阻塞。

## 4. 確認後直接實作到完成

1. 確認 remote 指向我自己的新 Repo，而不是模板來源 Repo。
2. 主要透過 `site-config.js` 客製，保留固定 8 個 `folderSlots`，以 `enabled` 控制顯示數量。
3. 不要用 Drive 資料夾名稱當程式 key；使用 `folderId`。
4. `items[].category` 對應槽位的內部 `id`，不是資料夾名稱。
5. **一般照片／影片不要建立或要求使用者填寫每張照片的 `title`、`date`、說明文字。** 相簿卡片只顯示媒體本身；Lightbox 只需要張數索引、下載與必要操作。原始 `fileName` 可保留供下載，但不要硬顯示成照片標題。
6. Google Drive 圖片使用真實 `fileId`：
   - 卡片縮圖：`https://drive.google.com/thumbnail?id=FILE_ID&sz=w800`
   - Lightbox 大圖：`https://drive.google.com/thumbnail?id=FILE_ID&sz=w1600`
   - 下載：`https://drive.google.com/uc?export=download&id=FILE_ID`
7. 影片要實際播放；優先使用穩定的 Drive preview，並實測。
8. 影片卡片要有封面，可使用 `posterFileId`。
9. 需要 AI 提示詞的分類，每張必須有完整 `prompt`；卡片只額外顯示「複製提示詞」操作，不要把完整提示詞預覽當成照片說明文字塞在卡片下方。
10. 響應式照片牆統一為：手機一列一張、接近內容滿寬；桌機充分利用可視寬度自然多欄，不要被窄固定容器卡住。
11. 不要顯示傳統「上一頁／下一頁」。大量媒體採增量載入：手機每批約 12 筆；桌機依寬度約 20／25／30 筆，接近底部自動追加。若有後端 API，底層仍用 cursor 分批取得，不可一次抓完整分類。
12. 首屏可視範圍可 eager 載入，其餘圖片使用 lazy loading；追加資料不得重複卡片、重置捲動位置或讓 Lightbox 索引失效。
13. 不可 commit Service Account JSON、API Key、Token、Cookie、`.env`、Secrets 或私人資料。
14. 更新 README，讓不會寫程式的人看得懂 8 槽位、Drive ID、分類名稱、媒體與部署如何修改。

## 5. 圖片素材品質：一定要檢查「來源黑邊」

不要只看 CSS。網站卡片雖然使用 `object-fit: cover`，但來源檔案如果本身帶黑色畫布，黑邊仍可能出現在縮圖或 Lightbox。

對所有圖片實際檢查：

- 是否有右側、下方或四周嵌入式黑邊／黑色 padding
- 如果只是連續純黑外框，只裁掉黑框，不要重畫人物、不要改內容
- 修正後重新從 Google Drive 載入驗證，不能只驗本機檔

## 6. 必須做真實瀏覽器驗收

至少測：

- 1440×900
- 1280×720
- 390×844

逐項驗證：

- 未輸入密碼不能進入
- 錯誤密碼不能進入
- 正確密碼可進入
- 正確密碼登入後，關閉並重開瀏覽器仍可在 90 天內自動進入；登出後重新整理必須再次輸入密碼
- 標題右側「所有照片」實際開啟根 Drive，而不是目前分類；根 Drive 可看到 01～08 八個子資料夾
- 只顯示 `enabled: true` 的分類；停用槽位不可出現
- 每個啟用分類的數量正確，Tab 數字不可寫死
- `folderSlots` 01～08 都保留正確 `folderId`；網站不另外顯示分類級 Drive 入口
- 一般照片／影片卡片**沒有**人工標題、日期、分類標籤或說明文字
- AI 類卡片只有媒體＋必要的「複製提示詞」操作，不顯示整段提示詞預覽
- 所有 Google Drive 圖片實際解碼成功
- 圖片卡片沒有來源檔造成的異常黑邊
- 影片實際載入並能播放，不只是顯示封面
- 需要提示詞的每張都有提示詞，複製按鈕可用
- 最新／最舊排序正常
- Lightbox 可開／關，上一個／下一個正常，而且只顯示索引／下載／必要操作，不硬造照片標題
- 390×844 手機照片牆為單欄；1280×720、1440×900 桌機為多欄，左右保留合理邊距
- 不出現傳統分頁按鈕；超過首批數量時，手機首批約 12 筆、桌機首批約 20／25／30 筆，捲到底會自動追加
- 追加後沒有重複卡片、跳回頁首、清空已載入內容或破壞 Lightbox 上一張／下一張
- 下載正常
- 10 套配色可輪替，重新整理後仍記得
- 手機沒有水平溢出、文字裁切、按鈕過小或被遮住
- Console / page error / network error 沒有未處理問題

有問題就修，再重跑；不要把失敗測試包裝成完成。

## 7. Git、部署與最後回報

驗收通過後：

1. 做去敏與 Secrets 掃描。
2. Commit / Push 到我的 `main`。
3. 依選定方式部署。
4. 線上版本再驗證啟用分類、Drive 媒體、影片、提示詞、密碼與黑邊。
5. 最後回報：
   - GitHub Repo 網址
   - 線上相簿網址
   - 密碼
   - Google Drive 根資料夾
   - 8 個槽位的啟用／停用狀態
   - 各啟用分類的數量
   - QA 測過哪些 viewport / 操作
   - 已知限制

**目標：讓不會寫程式的人只要回答問題，就能把這個最多 8 分類的模板完整變成自己的相簿網站。**

---
