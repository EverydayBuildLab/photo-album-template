# 給 Codex：用問答方式把「8 槽位相簿模板」改成我的版本

這份檔案已放在你從 GitHub Template 建立的新 Repo 裡。

把下面整段交給 Codex / Coding Agent 即可。

---

你現在要協助我把這個相簿 DEMO 客製成我自己的網站。

**工作原則：先用少量問題釐清需求，再直接做到完成。不要只給規劃，也不要一直停下來要求我確認。只有遇到登入、授權、缺資料或不可逆操作時才停下。**

## 0. 先確認這是我的新 Repo

1. 先確認目前工作目錄的 Git remote 指向我自己的 Repository。
2. 建議來源是 GitHub 的 **Use this template → Create a new repository**，不是直接修改公開模板。
3. 不可修改模板來源 Repo。
4. 預設只使用 `main` branch，不要為一般客製建立很多 branches，除非我明確要求。
5. 若目前資料夾還沒有 Git，可以協助初始化；若已經是我自己的 Repo，就沿用。

## 1. 先理解模板

模板在 `site-config.js` 的 `folderSlots` 預留 **01～08 共 8 個邏輯槽位**。

- `enabled: true`：網站顯示這個分類。
- `enabled: false`：保留槽位但不顯示。
- DEMO 目前只啟用前四個：大家、個人、影片、AI後製。
- 使用者可改成 1～8 個分類，不必保留這四個名稱。

公開 DEMO 預設使用 `assets/demo/` 內建素材，避免綁定模板維護者的私人 Google Drive。

客製時如果改用 Google Drive：

- Google Drive 資料夾名稱不是程式綁定鍵。
- 每個槽位以真實 `folderId` 綁定資料夾。
- 網站按鈕顯示名稱由 `label` 控制。
- Drive 資料夾可自由改名，不要要求一定叫 `01-大家`、`02-個人`。

## 2. 用問答方式收集需求

請一輪一輪問我，**每輪最多 3 個問題**。每輪得到答案後先簡短整理目前決定，資訊不足再問下一輪。

至少確認：

### A. 網站定位

- 相簿主題是什麼？
- 網站標題、副標、簡短說明要寫什麼？
- 是否保留 DEMO／公開展示字樣？

### B. 分類槽位 1～8

先問我要幾個分類，再逐類確認：

- 網站顯示名稱 `label`
- 圖片還是影片
- 單位（張／部／其他）
- 素材數量
- 是否需要 AI 提示詞功能

如果某分類是 AI 後製或其他 AI 風格分類，確認是否每一張都需要提示詞。需要時，每筆都要有 `prompt`，網站也要能預覽與複製。

### C. 素材來源

先問我要使用：

1. Google Drive（推薦給想自己管理照片的人）
2. 直接放在 Repo / 靜態網站內
3. 其他合法公開來源

如果使用 Google Drive：

- 請我提供根資料夾分享網址。
- 對每個啟用槽位確認或建立對應資料夾。
- 使用真實 `folderId`、`fileId`，不要用本機假 ID 冒充。
- Drive 資料夾名稱可自由命名。
- 公開網站必須能讀取媒體。
- 提醒我不要把私人內容誤設公開。

### D. 密碼與功能

- 進站密碼要設什麼？
- 預設保留：分類切換、最新／最舊、Lightbox、上一個／下一個、下載、影片播放器、手機響應式。
- 若分類需要 AI 提示詞，保留提示詞預覽與複製。
- 預設保留 10 套配色，使用 `localStorage` 記住配色。

提醒：純前端密碼只是展示門檻，不是真正安全保護。

### E. 視覺方向

用白話問我：

- 清爽明亮／深色質感／溫暖生活／極簡中性／其他
- 手機上最在意照片大小、按鈕、分類或文字密度哪一項

### F. 部署

問我要：

1. GitHub Pages
2. Cloudflare Pages / Workers
3. Netlify / Vercel
4. 只要本機

沒有偏好時優先 GitHub Pages。

## 3. 問完先給一次確認摘要

開始改檔前，用很短的方式整理：

- 主題／標題
- 啟用幾個槽位
- 每個槽位：序號、網站名稱、圖片／影片、數量、是否需提示詞、素材來源
- Google Drive 資料夾（若使用）
- 密碼／功能
- 視覺
- 部署

只問一次：

> 以上是否正確？若正確我就直接做到完成。

確認後，不要每一步停下來問是否繼續。

## 4. 確認後直接實作

1. 確認 remote 指向我的 Repo。
2. 主要透過 `site-config.js` 客製，保留固定 8 個 `folderSlots`，以 `enabled` 控制顯示數量。
3. `items[].category` 對應槽位內部 `id`。
4. 若用本地素材：
   - 圖片使用 `src`
   - 影片使用 `videoSrc`
   - 影片封面使用 `poster`
   - 下載使用 `download`
5. 若用 Google Drive：
   - 圖片顯示：`https://drive.google.com/thumbnail?id=FILE_ID&sz=w1600`
   - 影片預覽：`https://drive.google.com/file/d/FILE_ID/preview`
   - 下載：`https://drive.google.com/uc?export=download&id=FILE_ID`
   - 分類資料夾使用真實 `folderId`
6. 影片必須實際能播放，不能只顯示封面。
7. 需要 AI 提示詞的分類，每張必須有完整 `prompt`。
8. 更新 README，讓不會寫程式的人看得懂目前專案怎麼維護。

## 5. 隱私與去敏是硬性要求

在 Commit / Push / 部署前，必須檢查：

- 不可包含模板維護者或其他人的私人 email、帳號、真實公司內部資訊、本機使用者路徑。
- 不可提交 Service Account JSON、API Key、Token、Cookie、`.env`、私鑰、Credentials。
- 不要把舊專案中可能有敏感資料的 `.git` 歷史複製進新公開 Repo。
- 圖片／影片公開前應移除不必要 metadata。
- 截圖、README、Prompt、測試報告也要檢查，不只程式碼。
- 如果發現疑似敏感值，不要自行猜它是否可公開；先遮蔽或改用 placeholder。

## 6. 圖片素材品質：檢查來源黑邊

網站卡片雖然使用 `object-fit: cover`，但來源檔案若本身帶黑色畫布，黑邊仍可能出現。

對所有圖片檢查：

- 右側、下方或四周是否有嵌入式黑邊／padding
- 如果只是連續純黑外框，只裁掉黑框，不要重畫人物或內容
- 修正後重新從實際部署來源載入驗證

## 7. 必須做真實瀏覽器驗收

至少測：

- 1440×900
- 1280×720
- 390×844

逐項驗證：

- 未輸入密碼不能進入
- 錯誤密碼不能進入
- 正確密碼可進入
- 只顯示 `enabled: true` 的分類
- 每個啟用分類數量正確，Tab 數字不可寫死
- 所有圖片實際解碼成功
- 圖片卡片沒有來源黑邊
- 影片實際載入並可播放
- 需要提示詞的每張都有提示詞，複製按鈕可用
- 最新／最舊排序正常
- Lightbox 可開／關，上一個／下一個正常
- 下載來源正確
- 10 套配色可輪替，重新整理後仍記得
- 手機沒有水平溢出、文字裁切、按鈕太小或被遮住
- Console / page error / network error 沒有未處理問題

有問題就修，再重跑；不要把失敗測試包裝成完成。

## 8. Git、部署與最後回報

驗收通過後：

1. 做最後一次去敏與 Secrets 掃描。
2. Commit / Push 到我的 `main`。
3. 依選定方式部署。
4. 線上版本再驗證分類、媒體、影片、提示詞、密碼與黑邊。
5. 最後回報：
   - GitHub Repo 網址
   - 線上相簿網址
   - 密碼
   - 素材來源
   - 8 個槽位啟用／停用狀態
   - 各啟用分類數量
   - QA 測過哪些 viewport / 操作
   - 已知限制

**目標：讓不會寫程式的人只要回答問題，就能把最多 8 分類的模板完整變成自己的相簿網站。**

---
